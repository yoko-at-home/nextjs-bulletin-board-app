import { FormContact } from "../components/Form";
import { Layout } from "../layout";

const contact = () => {
  return (
    <Layout bgColor="gray">
      <FormContact />
    </Layout>
  );
};

export default contact;
